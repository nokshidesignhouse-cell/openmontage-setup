# 🎬 OpenMontage Windows সেটআপ গাইড

**Windows ব্যবহারকারীদের জন্য সহজ ধাপে ধাপে গাইড**

---

## ✅ প্রয়োজনীয় সফটওয়্যার

### 1️⃣ Python 3.8+ ইনস্টল করুন

**ডাউনলোড লিঙ্ক:** https://www.python.org/downloads/

**ইনস্টলেশন ধাপ:**
1. লিঙ্কে গিয়ে "Download Python" ক্লিক করুন
2. ইনস্টলার ডাউনলোড করুন এবং খুলুন
3. **অত্যন্ত গুরুত্বপূর্ণ:** চেকবক্স টিক করুন: ✅ "Add Python to PATH"
4. "Install Now" ক্লিক করুন
5. সম্পূর্ণ হওয়ার অপেক্ষা করুন

**যাচাই করুন:**
- `Windows + R` চাপুন
- `cmd` টাইপ করুন এবং Enter চাপুন
- এই কমান্ড চালান:
  ```
  python --version
  ```
- যদি সংস্করণ দেখা যায়, তাহলে ✅ সফল!

---

### 2️⃣ Git ইনস্টল করুন

**ডাউনলোড লিঙ্ক:** https://git-scm.com/download/win

**ইনস্টলেশন ধাপ:**
1. ডাউনলোড করুন এবং ইনস্টলার খুলুন
2. সব ডিফল্ট সেটিংস দিয়ে এগিয়ে যান (Next → Next → Next)
3. "Install" ক্লিক করুন
4. সম্পূর্ণ হওয়ার অপেক্ষা করুন

**যাচাই করুন:**
- কমান্ড প্রম্পট খুলুন (`cmd`)
- এই কমান্ড চালান:
  ```
  git --version
  ```
- যদি সংস্করণ দেখা যায়, তাহলে ✅ সফল!

---

## 🚀 OpenMontage সেটআপ করুন

### ধাপ 1: রিপোজিটরি ডাউনলোড করুন

**অপশন A: কমান্ড প্রম্পটে**
1. `Windows + R` চাপুন
2. `cmd` টাইপ করুন এবং Enter চাপুন
3. এই কমান্ড কপি-পেস্ট করুন:
   ```bash
   git clone https://github.com/nokshidesignhouse-cell/openmontage-setup.git
   cd openmontage-setup
   ```

**অপশন B: ম্যানুয়ালি**
1. https://github.com/nokshidesignhouse-cell/openmontage-setup এ যান
2. সবুজ "Code" বাটন ক্লিক করুন
3. "Download ZIP" ক্লিক করুন
4. ডাউনলোড সম্পূর্ণ হলে এক্সট্র্যাক্ট করুন
5. ফোল্ডার খুলুন

---

### ধাপ 2: স্বয়ংক্রিয় সেটআপ চালান

1. **`openmontage-setup` ফোল্ডার খুলুন**
2. **`setup.bat` ফাইল খুঁজুন**
3. **ডাবল-ক্লিক করুন** (শুরু হবে!)

**যা হবে:**
- ✅ Python যাচাই করবে
- ✅ Git যাচাই করবে
- ✅ ভার্চুয়াল এনভায়রনমেন্ট তৈরি করবে
- ✅ সব ডিপেন্ডেন্সি ইনস্টল করবে
- ✅ আউটপুট ফোল্ডার তৈরি করবে

**স্ক্রিন থেমে গেলে Enter চাপুন!**

---

## 🎬 প্রথম ভিডিও তৈরি করুন

### সহজ উপায়:

1. **`openmontage-setup` ফোল্ডার খুলুন**
2. **`run_openmontage.bat` ডাবল-ক্লিক করুন**
3. **মেনু থেকে "1" বেছে নিন** (Create a new video)
4. **আপনার ভিডিও আইডিয়া টাইপ করুন**
   
   উদাহরণ:
   ```
   সাদা আকাশ কেন নীল
   ```
   
   অথবা:
   ```
   ডিজিটাল মার্কেটিং শেখানোর একটি ৩০ সেকেন্ডের ভিডিও
   ```

5. **Enter চাপুন এবং অপেক্ষা করুন!** 🎥

---

## 📁 আপনার ভিডিও কোথায় আছে?

সেটআপ সম্পূর্ণ হলে:
- ফোল্ডার খুলুন: `openmontage-setup` → `output`
- আপনার সব ভিডিও এখানে থাকবে! 📹

---

## 🆘 সমস্যা হলে কি করবেন?

### সমস্যা: Python ইনস্টল নেই
```
❌ Python is not installed!
```

**সমাধান:**
1. Python ডাউনলোড করুন: https://www.python.org/downloads/
2. ইনস্���ল করুন এবং **PATH এ যোগ করুন** ✓
3. কম্পিউটার রিস্টার্ট করুন
4. `setup.bat` আবার চালান

---

### সমস্যা: Git ইনস্টল নেই
```
❌ Git is not installed!
```

**সমাধান:**
1. Git ডাউনলোড করুন: https://git-scm.com/download/win
2. ইনস্টল করুন
3. কম্পিউটার রিস্টার্ট করুন
4. `setup.bat` আবার চালান

---

### সমস্যা: "ModuleNotFoundError"
**সমাধান:**
1. কমান্ড প্রম্পট খুলুন
2. `openmontage-setup` ফোল্ডারে যান:
   ```bash
   cd openmontage-setup
   ```
3. এই কমান্ড চালান:
   ```bash
   venv\Scripts\activate.bat
   pip install -r requirements.txt
   ```

---

### সমস্যা: "Access denied" বা পারমিশন এরর
**সমাধান:**
1. কমান্ড প্রম্পট **অ্যাডমিনিস্ট্রেটর হিসেবে** খুলুন
2. সেটআপ আবার চালান

---

## ⚙️ এনভায়রনমেন্ট কনফিগার করুন (ঐচ্ছিক)

**উন্নত ভিডিও তৈরির জন্য API কী যোগ করুন:**

1. `.env` ফাইল খুলুন (নোটপ্যাড দিয়ে)
2. আপনার API কী যোগ করুন:

```bash
# Google Veo (উন্নত AI ভিডিও)
GOOGLE_VEO_API_KEY=আপনার_কী_এখানে

# OpenAI (চিন্তাশীল স্ক্রিপ্ট)
OPENAI_API_KEY=আপনার_কী_এখানে

# Runway (ভিডিও এডিটিং)
RUNWAY_API_KEY=আপনার_কী_এখানে
```

3. ফাইল সংরক্ষণ করুন (Ctrl + S)

---

## 📱 দ্রুত কমান্ড রেফারেন্স

| কমান্ড | কাজ |
|--------|-----|
| `setup.bat` | সম্পূর্ণ সেটআপ করুন |
| `run_openmontage.bat` | ভিডিও তৈরি করুন |
| `venv\Scripts\activate.bat` | ভার্চুয়াল এনভায়রনমেন্ট চালু করুন |
| `python main.py status` | সিস্টেম স্ট্যাটাস দেখুন |
| `python main.py config` | কনফিগারেশন দেখুন |

---

## 🔗 সহায়ক লিঙ্ক

- 🌐 [OpenMontage ওয়েবসাইট](https://openmontage.video/)
- 📺 [YouTube চ্যানেল](https://www.youtube.com/@OpenMontage)
- 💻 [GitHub রিপোজিটরি](https://github.com/calesthio/OpenMontage)
- 🐍 [Python ডাউনলোড](https://www.python.org/downloads/)
- 🔧 [Git ডাউনলোড](https://git-scm.com/download/win)

---

## 🎉 সফল সেটআপের চিহ্ন

✅ `setup.bat` সফল হয়েছে
✅ কোনো এরর মেসেজ নেই
✅ `run_openmontage.bat` মেনু দেখায়
✅ ভিডিও `output` ফোল্ডারে সংরক্ষিত হয়

---

**সবকিছু প্রস্তুত? এখন ভিডিও তৈরি করুন!** 🚀🎬

যদি কোনো সমস্যা হয়, GitHub Issues এ প্রশ্ন করুন!
